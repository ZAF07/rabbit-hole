# Agent skills

Per-repo configuration the engineering skills read from. Each topic has its own file — open the one you need.

## Issue tracker

Issues and PRDs are tracked as local markdown files under `.scratch/<feature>/`. See [`issue-tracker.md`](issue-tracker.md).

## Triage labels

Five canonical triage roles, using the default label strings. See [`triage-labels.md`](triage-labels.md).

## Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See [`domain.md`](domain.md).
